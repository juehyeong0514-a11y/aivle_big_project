# Debug Journal — mobile ID scan Not Found

Started: 2026-07-30
Goal: Keep mobile ID scanning usable when automatic detection is unavailable and replace PC ID capture with a temporary completion action.

## Environment snapshot
- Runtime: React 19 / Vite 8 frontend and Express backend.
- Route: `/mobile/id-scan` is registered and QR generation points to the same path.
- API: mobile page calls `/api/mobile-id-scans/:token/detect`; an older OCR deployment without `/ocr/id-card/detect` returns `404 Not Found`.

## Hypotheses
1. [CONFIRMED] The displayed Not Found comes from the optional automatic-detection endpoint missing in the deployed OCR service; the React page route itself exists.
2. [REFUTED] The QR path and React route differ; both use `/mobile/id-scan`.
3. [REFUTED] PC candidate authorization blocks the mobile page; the mobile scan route is public and token-based.

## Artifacts to revert
- [ ] `.debug-journal.md` — remove before final handoff.
- [ ] local Vite preview process on port 4173 — stop before final handoff.

## Findings
- The automatic detector treats a missing optional endpoint as a blocking page error even though manual capture remains available.
